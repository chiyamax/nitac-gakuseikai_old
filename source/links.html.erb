---
title: リンク
---

<h2>リンク</h2>

<section>
  <h3>高等専門学校</h3>

  <% data.links.schools.each_slice(3).to_a.each do |threeschools| %>
    <ul class="links row">
      <% threeschools.each do |school| %>
        <li class="col-md-4">
          <a href="<%= school[:url] %>" target="_blank" class="links">
            <div class="card link-card">
              <img src="../images/<%= school[:img] %>" class="link-img">
              <%= school[:name] %>
            </div>
          </a>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>

<section>
  <h3>学生会・学友会</h3>
  <% data.links.associations.each_slice(3).to_a.each do |threeassociations| %>
    <ul class="links row">
      <% threeassociations.each do |association| %>
        <li class="col-md-4">
          <a href="<%= association[:url] %>" target="_blank" class="links">
            <div class="card link-card">
              <img src="../images/<%= association[:img] %>" class="link-img">
              <%= association[:name] %>
            </div>
          </a>
        </li>
      <% end %>
    </ul>
  <% end %>
</section>

